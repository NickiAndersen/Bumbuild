# 🔑 Keys

Place your Android signing keystores (`.jks` / `.keystore`) in this folder.

**Bumbuild** will automatically detect them when setting up Android signing.

> ⚠️ This folder is git-ignored. Never commit keystore files to version control.  
> Keep a secure backup of your keystores, losing them means you can't update your app on Google Play.
